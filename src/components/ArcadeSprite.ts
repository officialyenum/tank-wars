import {
    defineComponent,
    Types,
} from 'bitecs'

export const ArcadeSprite = defineComponent({
    texture: Types.ui8,
})

export const ArcadeSpriteStatic = defineComponent({
    texture: Types.ui8,
})